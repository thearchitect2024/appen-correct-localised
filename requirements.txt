# Core dependencies
flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0
jsonschema==4.20.0
sendgrid==6.10.0

# vLLM for local GPU inference (REQUIRED)
vllm==0.6.3
# Note: flash-attn must be installed separately with: pip install flash-attn --no-build-isolation
# flash-attn==2.6.3  # Install manually with --no-build-isolation flag
transformers>=4.45.0
torch>=2.1.0
pyngrok==7.0.1  # For SageMaker testing with public URLs

# Redis/Valkey cache support
redis==5.0.1
valkey==6.1.1

# Language detection (optional - at least one recommended)
langdetect==1.0.9
lingua-language-detector==2.0.2

# Production server
gunicorn==21.2.0
waitress==3.0.0

# Development and testing
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0

# Optional development tools
pre-commit==3.6.0 