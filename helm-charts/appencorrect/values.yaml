# Default values for appencorrect
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 3

image:
  repository: 411719562396.dkr.ecr.us-east-1.amazonaws.com/sapling
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "1a846cda4054a25a2ebd6f2055c30bb92ff5f14d"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  fsGroup: 1001
  runAsNonRoot: true
  runAsUser: 1001

securityContext: {}

service:
  type: ClusterIP
  port: 80
  targetPort: 5006

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: appencorrect.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: appencorrect-tls
  #    hosts:
  #      - appencorrect.local

resources:
  limits:
    cpu: "2"
    memory: 4Gi
  requests:
    cpu: "1"
    memory: 2Gi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector:
  app: appencorrect

tolerations:
- effect: NoSchedule
  key: app
  operator: Equal
  value: appencorrect

affinity: {}

# Environment variables
env:
  GEMINI_API_KEY: "AIzaSyDqM27YJjuNd1f_gnylCsnqjvEijqLRX7c"
  # Database configuration
  DATABASE_PATH: "/app/data/api_keys.db"

# Health check configuration
livenessProbe:
  httpGet:
    path: /health
    port: 5006
    scheme: HTTP
  initialDelaySeconds: 30
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 3
  successThreshold: 1

readinessProbe:
  httpGet:
    path: /health
    port: 5006
    scheme: HTTP
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 5
  failureThreshold: 3
  successThreshold: 1

# Rolling update strategy
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%

# Termination grace period
terminationGracePeriodSeconds: 30

# Database persistence configuration
persistence:
  enabled: true
  storageClass: "gp2"
  accessMode: ReadWriteOnce
  size: 100Gi
  annotations: {}
  # Optional: existing PVC name
  existingClaim: ""

